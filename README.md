# Ecommerce-django

## API

`pip install virtualenv`

`cd backend`

`virtualenv venv`

`source env/bin/active`

`pip install -r requirements.txt`

`python manage.py runserver`

## Front

`cd frontend`

`npm install`

`npm run dev`
